# KDI Asset Management AI — New PC Setup Report

- **Date:** 2026-09-10
- **Project root:** `C:\Users\user\Projects\Asset_Management_AI`
- **Machine:** Windows 11 Home (new development PC)
- **Old PC:** OFF — this PC must operate fully standalone. No old-PC service was contacted.

> No secrets are printed anywhere in this report. Only presence/absence and non-sensitive names are recorded.

---

## 1. Git

| Item | Value |
|---|---|
| Repo root | `C:\Users\user\Projects\Asset_Management_AI` |
| Branch | `ai-search-v3-job1-safety` (matches expected) |
| Remote | `origin` → `https://github.com/klinikdrinder/Asset_Management_AI.git` |
| Working tree | CHANGED (see below) — no reset/discard performed |

Uncommitted changes (pre-existing / expected):
- `dashboard/package-lock.json` — modified
- `dashboard/AGENTS.md`, `dashboard/CLAUDE.md` — untracked; auto-generated by `next dev` (see AGENTS.md note)

## 2. Node / npm / dashboard install

| Item | Value |
|---|---|
| Node | v26.3.0 (satisfies `engines: >=22.13.0`) |
| npm | 11.16.0 |
| Locked-file issue (EPERM/SWC) | RESOLVED — no stale KDI node processes; the only node PID at start was the Claude Code heartbeat, not the dashboard |
| Clean | `node_modules` + `.next` removed cleanly (no `cmd /c rmdir` fallback needed) |
| npm cache | verified OK |
| `npm ci` | **PASS** — 799 packages, no EPERM/SWC lock error |
| `npm ls --depth=0` | Clean — all top-level deps present, no unmet/invalid |

**Note (non-blocking):** newer npm gated 9 postinstall scripts (`allow-scripts`), e.g. esbuild/firebase. Verified this did **not** break the toolchain — tsx/esbuild run fine (69 unit tests pass). Run `npm approve-scripts` only if a future native-binary issue appears.

## 3. Python environments

| Env | Python | pip check | Notes |
|---|---|---|---|
| `.venv` (main) | 3.14.6 | No broken requirements | Runs the semantic **indexing worker** (`scripts/run_semantic_indexing.py`) and the npm `visual:*` scripts |
| `.venv-semantic` | 3.12.10 | No broken requirements | OpenCLIP / ML runtime |

Semantic imports in `.venv-semantic` — **all PASS**: torch 2.3.1+cpu, numpy 1.26.4, transformers 4.57.6, accelerate 1.10.1, PIL 11.3.0, cv2 4.10.0, onnxruntime 1.29.0, sentence_transformers 5.1.0, open_clip 3.3.0, supabase 2.17.0.

Semantic worker import (`run_semantic_indexing.py --help`, main `.venv`, provider-free/read-only) — **PASS** (all `kdi_media.*` modules import).

## 4. Environment files & secrets  — ⚠️ NOT RESTORED

| Path | Status |
|---|---|
| `.env` | **MISSING** |
| `.env.local` | **MISSING** |
| `dashboard/.env.local` | **MISSING** |
| `dashboard/.env` | **MISSING** |
| `.secrets/` (directory) | **MISSING** |
| `.secrets/firebase-admin.json` | **MISSING** |
| `.secrets/kdi-media-reader.json` | **MISSING** |
| `.env.example`, `dashboard/.env.example` | present (templates only) |

No OS-level env vars are set either (checked `SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `GOOGLE_APPLICATION_CREDENTIALS`).

**This is the primary blocker.** The environment configuration described as "restored" is not present on disk. Everything data-backed (Supabase, Firebase auth, Google Drive media) is inoperable until these are placed.

Required variable names still missing (from the two `.example` files; values are the user's to restore):
- Root `.env`: `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `GOOGLE_DRIVE_SERVICE_ACCOUNT_CREDENTIALS_PATH` (+ Google Drive folder IDs; Ollama/OpenAI have defaults/optional)
- `dashboard/.env.local`: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` (or `NEXT_PUBLIC_SUPABASE_ANON_KEY`), `SUPABASE_SERVICE_ROLE_KEY`, `NEXT_PUBLIC_FIREBASE_*` (api key, auth domain, project id, storage bucket, sender id, app id), `FIREBASE_PROJECT_ID`, `GOOGLE_APPLICATION_CREDENTIALS` (→ `../.secrets/firebase-admin.json`), plus `KDI_MASTER_DRIVE_ID` / Drive service-account for media delivery.

## 5. Supabase

- Configuration present: **NO** (no URL/keys on this PC)
- Direct connection / read-only test: **NOT TESTED — BLOCKED** (no configuration; none invented)
- Proven at runtime: dashboard returns HTTP 500 `"Authentication configuration is unavailable"` from `getSupabasePublicConfig` (the Supabase session middleware).

## 6. Firebase / Google credentials

- Required: **YES** (Firebase Admin preflight at boot in `instrumentation.ts`; Google Drive service account for media)
- Credential file `.secrets/firebase-admin.json`: **does not exist**
- Path valid: **NO** (directory missing)
- No write requests were made.

## 7. Semantic worker / services (local runtime)

| Service | Entry point | Port | Status |
|---|---|---|---|
| Semantic indexing worker | `scripts/run_semantic_indexing.py` (main `.venv`) | — (batch CLI) | Import **PASS**; needs Supabase + Drive creds to run. Not run (no indexing). |
| Visual indexing HTTP service | `uvicorn visual_indexing.service:app` (npm `visual:service`, main `.venv`) | **8765** | **CANNOT START** — `fastapi` + `uvicorn` are in **neither** requirements file nor either venv. OpenCLIP runtime itself imports OK in `.venv-semantic`. |
| Ollama (embeddings/description) | external local service | **11434** | **NOT INSTALLED** — required for local semantic query embeddings; search degrades to text/metadata ranking without it. |

Health endpoints exist: FastAPI `/health` + `/embed-text` (lazy model load — safe to import), dashboard `/api/health` (returns 503 without Supabase).

## 8. Search / migration code

- Canonical search API: `dashboard/app/api/search/route.ts` (POST). `dashboard/app/api/search/v3/route.ts` is a **deprecated alias** that re-exports it. Retrieval goes through the canonical search-authority module (Supabase `hybrid_search_assets` RPC per migrations). Source present.
- People/Appearance foundation migration `supabase/migrations/20260910030436_fix02_people_appearance_foundation.sql` — **EXISTS**. (Not applied — no migrations run.)

## 9. Tests (safe / local only)

| Suite | Result |
|---|---|
| Dashboard unit (env-independent subset: 9 files) | **69 passed, 0 failed** (dashboard-utils, media-repository, safe-deployment, user-management-rbac, media-library, media-range, thumbnail-cache, video-poster, dev-preview) |
| Full `npm run unit` | Skipped — several files block on Supabase with no env (hangs); toolchain confirmed working |
| Python `test_*.py` | Skipped — pytest not in `.venv`; most tests require Supabase credentials |

## 10. Dashboard runtime

| Item | Value |
|---|---|
| Start command | `npm run dev` (`next dev`) |
| Started with | `KDI_LIBRARY_DEV_BYPASS=true` (documented dev flag) to skip the Firebase Admin boot preflight, since creds are missing |
| Listening | **http://localhost:3000** (IPv6 `::3000`) |
| Server boot | PASS (Next.js compiles + serves HTML shell) |
| Route responses | **HTTP 500 on all routes** — `"Authentication configuration is unavailable"` (Supabase env missing) |

### Routes identified
- KDI Central Library (production): `dashboard/app/library/page.tsx` → `/library` (+ `/library/files`, `/library/folders`). **Auth-required** (`requireProtectedPage()`).
- Dev library: `/dev/library` (`dashboard/app/dev/library/page.tsx`).
- Local preview library (no Firebase): `/dev/library-preview` — gated by `KDI_LOCAL_LIBRARY_PREVIEW=true`; still needs Supabase service client.
- Semantic search: `/api/search` (POST), alias `/api/search/v3`.

## 11. Hard-coded `D:\Asset_Management_AI` paths

Total files referencing `D:\Asset_Management_AI`: **41**. Most are historical `reports/*.md` and rollout `*.json` artifacts (no change needed). Active/runtime-relevant items requiring migration to `C:\Users\user\Projects\Asset_Management_AI`:

| File | Line | Nature | Impact |
|---|---|---|---|
| `scripts/run-repository-maintenance.ps1` | 4 | `$root = 'D:\Asset_Management_AI'` (+ `.venv` python) | **Broken** scheduled-maintenance script on this PC |
| `scripts/install-kdi-repository-maintenance-task.ps1` | 3 | `$root = 'D:\Asset_Management_AI'` | Would install a **broken** scheduled task |
| `src/kdi_media/google_drive.py` | 411 | Drive service-account creds **fallback default** → `D:\...\.secrets\kdi-media-reader.json` | Wrong when `GOOGLE_DRIVE_SERVICE_ACCOUNT_CREDENTIALS_PATH` unset |
| `dashboard/app/lib/firebase/admin-credentials.ts` | 24 | Stale first candidate path | Low — cwd-relative fallback (`..\.secrets\firebase-admin.json`) already covers the C: path |
| `dashboard/app/auth/callback/diagnostics.ts` | 16 | Auth-log dir default → `D:\...\dashboard\.logs` | Minor — overridable via `KDI_AUTH_LOG_DIR`, wrapped in try/catch |
| `config/semantic-search/kdi_semantic_analyzer_v1.json`, `scripts/generate_ai_search_v3_job6_reports.py`, `scripts/deploy_step10_legacy_status_fix.py` | — | Indexing-time / report output paths | Low — only relevant during indexing/reporting |

No source files were edited (report-only, per scope).

## 12. Scheduled tasks / synchronization on this PC

- **No KDI scheduled task is registered** (`Get-ScheduledTask` shows only generic Windows/Office maintenance tasks). No KDI repository-maintenance or synchronization job is configured on this new PC.
- The installer (`install-kdi-repository-maintenance-task.ps1`) must have its D: root corrected before it could be used here.

## 13. Local-only data still required on this PC

| Item | Status | Class |
|---|---|---|
| `dashboard/.env.local` (Supabase public + service + Firebase) | MISSING | **REQUIRED FOR DEVELOPMENT** |
| Root `.env` (Supabase, Drive, provider config) | MISSING | REQUIRED for indexing/Drive tooling |
| `.secrets/firebase-admin.json` | MISSING | REQUIRED for auth (or use dev bypass) |
| `.secrets/kdi-media-reader.json` (Drive read) | MISSING | REQUIRED for media thumbnails/preview/download |
| Ollama runtime (127.0.0.1:11434) | NOT INSTALLED | Needed for local semantic embeddings (optional for basic search) |
| `fastapi` + `uvicorn` in a venv | MISSING | Needed only for the visual-indexing HTTP service (port 8765) |
| Local models / media / test fixtures | Not required for dashboard dev | REQUIRED ONLY FOR FULL INDEXING |

## 14. Remaining blockers

1. **Environment configuration + `.secrets/` not restored** — restore `dashboard/.env.local` (+ root `.env`) and the `.secrets/` JSON credential files. Until then the dashboard 500s on every route and Supabase/Firebase/Drive are all inoperable.
2. **Ollama not installed** — install + `ollama serve` (port 11434) for local semantic query embeddings, or accept degraded text/metadata search.
3. **Visual indexing service unstartable** — add `fastapi`+`uvicorn` to the intended venv if the OpenCLIP HTTP service (port 8765) is needed.
4. **D: path migration** — fix the two `.ps1` maintenance scripts and `google_drive.py` fallback (and optionally the two dashboard TS constants) before relying on scheduled maintenance / Drive fallback on this PC.

## 15. Old-PC dependency

**None active.** No old-PC localhost service was contacted or is required. Once the env/secrets are restored and (optionally) Ollama is installed locally, this PC operates fully independently. The old PC is **not** required for normal development.

---

# PORTABILITY FIX (update — 2026-09-10)

Removed the remaining **active** hard-coded dependency on `D:\Asset_Management_AI` so the project is portable on any machine/clone location.

**Active old-PC runtime paths — before: 6, after: 0.**

## Files changed
| File | Change |
|---|---|
| `scripts/run-repository-maintenance.ps1` | `$root` now derived from `$PSScriptRoot` (scripts → repo root) with a `$MyInvocation` fallback |
| `scripts/install-kdi-repository-maintenance-task.ps1` | Same dynamic `$root`; installed task targets the current clone. **Not registered** (validation-only) |
| `src/kdi_media/google_drive.py` | New `default_service_account_credentials_path()` → `<PROJECT_ROOT>/.secrets/kdi-media-reader.json` from `__file__`; precedence: arg → `GOOGLE_DRIVE_SERVICE_ACCOUNT_CREDENTIALS_PATH` → portable default |
| `dashboard/app/lib/firebase/admin-credentials.ts` | Removed D: absolute constant; approved fallback is cwd-relative `..\.secrets\firebase-admin.json`. Precedence/fail-closed behavior preserved |
| `dashboard/app/auth/callback/diagnostics.ts` | Default auth-log dir → `path.join(process.cwd(), ".logs")`; `KDI_AUTH_LOG_DIR` override preserved |
| `scripts/deploy_step10_legacy_status_fix.py` | `WORKSPACE` run-location guard now derived from `__file__` (parents[1]) |
| `dashboard/tests/firebase-admin-credentials.test.ts` | Rewritten to use synthetic temp credentials (no real creds, no D:) |
| `dashboard/tests/portability-paths.test.ts` | **New** — static scan asserting 0 active D: refs + dynamic-derivation markers |
| `tests/test_drive_credentials_portable.py` | **New** — Drive credential default is repo-relative + env precedence |
| `dashboard/package.json` | Added `tests/portability-paths.test.ts` to the `unit` script |

No source files were edited outside this list. No production data touched.

## Classification of remaining `D:\Asset_Management_AI` occurrences
| Occurrence | Class | Action |
|---|---|---|
| 6 active runtime files (above) | ACTIVE_RUNTIME_PATH | **Fixed → 0** |
| `dashboard/tests/*.test.ts` (3 refs) | TEST_ASSERTION | Kept — negative checks / scanner needle |
| `scripts/generate_ai_search_v3_job6_reports.py:132` | DOCUMENTATION | Kept — prose inside generated report text, not a file path |
| `config/semantic-search/kdi_semantic_analyzer_v1.json:13` `python_executable` | STALE_CONFIG_METADATA | Kept — **not consumed** by any runtime code (`run-semantic-worker.ps1` computes its own `$root`-relative python). Editing it would change the content-addressed analyzer fingerprint, so left for a deliberate version bump if desired |
| ~33 files under `reports/**` (`*.json`, `*.md`) | HISTORICAL_REPORT | Kept unchanged |

## Tests
- Dashboard (`firebase-admin-credentials` + `portability-paths`): **12 passed, 0 failed**
- Python (`test_drive_credentials_portable`, main `.venv`, `PYTHONPATH=src`): **4 passed, 0 failed**
- Total: **16 passed, 0 failed**

## ENV (presence only — still not restored)
| File | Status |
|---|---|
| Root `.env` | MISSING |
| Root `.env.local` | MISSING |
| Dashboard `.env.local` | MISSING |
| Firebase credentials `.secrets/firebase-admin.json` | MISSING |
| Drive credentials `.secrets/kdi-media-reader.json` | MISSING |

## SUPABASE
NOT TESTED — ENV MISSING (no credentials restored; nothing invented)

## DASHBOARD
DEGRADED — boots on http://localhost:3000 but 500s until `dashboard/.env.local` (Supabase public config) is restored

## KDI CENTRAL LIBRARY
NOT TESTED — ENV MISSING (`/library` route present and auth-gated; cannot exercise without Supabase/Firebase config)

---

## Guardrails honored
- No production data modified. No Supabase schema/data changes. No migrations applied.
- No full semantic indexing; 877 assets untouched. No patient media downloaded.
- No secrets printed. Dashboard bound to localhost only.
