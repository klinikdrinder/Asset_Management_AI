import {readFile} from "node:fs/promises";import {resolve} from "node:path";const url=process.env.NEXT_PUBLIC_SUPABASE_URL,token=process.env.SUPABASE_DASHBOARD_ACCESS_TOKEN;if(!url||!token)throw Error("configuration unavailable");const ref=new URL(url).hostname.split(".")[0],migration="20260826043121_phase18_authorization_enforcement.sql",sql=await readFile(resolve(import.meta.dirname,"../../supabase/migrations",migration),"utf8"),r=await fetch(`https://api.supabase.com/v1/projects/${ref}/database/query`,{method:"POST",headers:{Authorization:`Bearer ${token}`,"Content-Type":"application/json"},body:JSON.stringify({query:sql})});if(!r.ok)throw Error(`PHASE18_MIGRATION_FAILED:${r.status}:${await r.text()}`);console.log(JSON.stringify({status:"APPLIED",migration,secrets_printed:false}));
