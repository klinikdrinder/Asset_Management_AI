export {};
const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.SUPABASE_DASHBOARD_ACCESS_TOKEN;if(!url||!key)throw Error("credentials unavailable");const ref=new URL(url).hostname.split(".")[0],q=`select n.nspname schema_name,p.proname function_name,pg_get_function_identity_arguments(p.oid) arguments from pg_proc p join pg_namespace n on n.oid=p.pronamespace where n.nspname='public' and (p.proname ilike '%search%' or p.proname ilike '%embedding%' or p.proname ilike '%vector%') order by p.proname`;const r=await fetch(`https://api.supabase.com/v1/projects/${ref}/database/query`,{method:"POST",headers:{Authorization:`Bearer ${key}`,"Content-Type":"application/json"},body:JSON.stringify({query:q})});if(!r.ok)throw Error(`${r.status}`);console.log(JSON.stringify(await r.json(),null,2));
