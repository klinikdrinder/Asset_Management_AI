import{readFileSync}from"node:fs";function env(file:string){return Object.fromEntries(readFileSync(file,"utf8").replace(/^\uFEFF/,"").split(/\r?\n/).flatMap(l=>{const m=l.match(/^([A-Za-z_][A-Za-z0-9_]*)=(.*)$/);return m?[[m[1],m[2].trim().replace(/^(['"])(.*)\1$/,"$2")]]:[]}))}const local=env(".env.local"),root=env("../.env.local"),ref=new URL(local.NEXT_PUBLIC_SUPABASE_URL!).hostname.split(".")[0],token=root.SUPABASE_DASHBOARD_ACCESS_TOKEN!;const query=`insert into public.approved_app_users(normalized_email,role,is_active,can_view_clinical,can_download) values ('kdi.auth.acceptance.20260813@example.com','STAFF',true,true,true) on conflict(normalized_email) do update set is_active=true,role='STAFF',can_view_clinical=true,can_download=true,updated_at=now()`;const response=await fetch(`https://api.supabase.com/v1/projects/${ref}/database/query`,{method:"POST",headers:{Authorization:`Bearer ${token}`,"Content-Type":"application/json"},body:JSON.stringify({query})});if(!response.ok)throw new Error(`approval failed (${response.status})`);console.log("TEST_APPROVAL=PASS");
