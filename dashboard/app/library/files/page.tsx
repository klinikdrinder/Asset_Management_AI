import{requireProtectedPage}from"../../auth";import{FilesPage}from"../../library-ui";import{getPreviewRole}from"../../lib/dev-preview";import{PreviewLibrary}from"../../preview/preview-library";export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){await requireProtectedPage();const preview=await getPreviewRole();return preview?<PreviewLibrary role={preview}/>:<FilesPage query={await searchParams}/>}
