import{notFound}from"next/navigation";import{requireProtectedPage}from"../../../auth";import{FilesPage}from"../../../library-ui";import{getSourceFolderById}from"../../../queries";export default async function Page({params,searchParams}:{params:Promise<{sourceFolderId:string}>;searchParams:Promise<Record<string,string|undefined>>}){await requireProtectedPage();const id=(await params).sourceFolderId;if(!(await getSourceFolderById(id)).data)notFound();return <FilesPage folderId={id} query={await searchParams}/>}
