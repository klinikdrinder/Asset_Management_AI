"use client";
import Link from"next/link";import{FormEvent,useState}from"react";import{createPasswordAuthClient}from"../lib/supabase/auth-client";
export function SignupForm(){const[name,setName]=useState(""),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[confirm,setConfirm]=useState(""),[message,setMessage]=useState(""),[error,setError]=useState(""),[pending,setPending]=useState(false);async function submit(e:FormEvent){e.preventDefault();setError("");setMessage("");if(password!==confirm){setError("Passwords do not match.");return}if(password.length<8){setError("Password must be at least 8 characters.");return}setPending(true);const auth=createPasswordAuthClient(true);const result=await auth.auth.signUp({email:email.trim(),password,options:{data:{full_name:name.trim()},emailRedirectTo:`${window.location.origin}/login`}});if(result.error){setError(result.error.message);setPending(false);return}if(result.data.session){const linked=await auth.rpc("link_current_app_user");if(!linked.error){window.location.replace("/library");return}await auth.auth.signOut()}setMessage("Account created. Check your email to confirm your address. Access begins after KDI approval.");setPending(false)}return <form className="authForm" onSubmit={submit}><label>Full Name<input name="fullName" autoComplete="name" required value={name} onChange={e=>setName(e.target.value)}/></label><label>Email<input name="email" type="email" autoComplete="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input name="password" type="password" autoComplete="new-password" minLength={8} required value={password} onChange={e=>setPassword(e.target.value)}/></label><label>Confirm Password<input name="confirmPassword" type="password" autoComplete="new-password" minLength={8} required value={confirm} onChange={e=>setConfirm(e.target.value)}/></label>{error&&<div className="authError" role="alert">{error}</div>}{message&&<div className="authSuccess" role="status">{message}</div>}<button className="authPrimary" disabled={pending}>{pending?"Creating account…":"Create Account"}</button><div className="authLinks"><Link href="/login">Back to Login</Link></div></form>}
