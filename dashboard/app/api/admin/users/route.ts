import{NextRequest,NextResponse}from"next/server";import{AuthenticationRequired,AuthorizationDenied,requireAdmin}from"../../../auth";import{allowedInvitationRoles,canInvite}from"../../../lib/management-rbac";import{createServiceClient}from"../../../lib/supabase/service";import{managementAudit,normalizedEmail,publicOrigin,sameOrigin,validEmail}from"../../../lib/user-management";
export const runtime="nodejs";const noStore={"Cache-Control":"private, no-store"};
export async function GET(){try{const actor=await requireAdmin(),client=createServiceClient();const[{data:profiles,error:pError},{data:invites,error:iError},auth]=await Promise.all([client.from("app_users").select("user_id,email,management_role,is_active,created_at,invited_at,invited_by,disabled_at"),client.from("user_invitations").select("id,auth_user_id,email,assigned_role,status,invited_by,invited_at,last_sent_at,accepted_at,send_count"),client.auth.admin.listUsers({page:1,perPage:1000})]);if(pError||iError||auth.error)throw pError??iError??auth.error;const authById=new Map(auth.data.users.map(u=>[u.id,u])),profileById=new Map((profiles??[]).map(p=>[p.user_id,p])),rows=(profiles??[]).map(p=>{const identity=authById.get(p.user_id),invitation=(invites??[]).find(i=>i.auth_user_id===p.user_id);return{id:p.user_id,email:p.email,role:p.management_role,status:!p.is_active?(invitation?.status==="pending"?"Invitation Pending":"Disabled"):"Active",invitationStatus:invitation?.status??(identity?.email_confirmed_at?"accepted":null),createdAt:p.created_at,invitedAt:p.invited_at,lastSignInAt:identity?.last_sign_in_at??null,invitedBy:p.invited_by?profileById.get(p.invited_by)?.email??null:null,isActive:p.is_active,invitationId:invitation?.id??null,lastSentAt:invitation?.last_sent_at??null,sendCount:invitation?.send_count??0,canManage:actor.managementRole==="super_admin"?p.management_role!=="super_admin":p.management_role==="user"}});return NextResponse.json({users:rows,actorRole:actor.managementRole,allowedRoles:allowedInvitationRoles(actor.managementRole)},{headers:noStore})}catch(error){const status=error instanceof AuthenticationRequired?401:error instanceof AuthorizationDenied?403:500;return NextResponse.json({error:status===401?"Authentication required.":status===403?"You do not have permission to access user management.":"User management is temporarily unavailable."},{status,headers:noStore})}}
export async function POST(request:NextRequest){let actor;try{actor=await requireAdmin()}catch{return NextResponse.json({error:"You do not have permission to invite users."},{status:403,headers:noStore})}if(!sameOrigin(request))return NextResponse.json({error:"Request could not be verified."},{status:403,headers:noStore});const body=await request.json().catch(()=>({}))as Record<string,unknown>,email=normalizedEmail(body.email),role=body.role;if(!validEmail(email))return NextResponse.json({error:"Enter a valid email address."},{status:400,headers:noStore});if(!canInvite(actor.managementRole,role)){await managementAudit(actor,"invitation_rejected",{targetEmail:email,roleAfter:String(role),outcome:"rejected",metadata:{reason:"role_not_allowed"}});return NextResponse.json({error:"You do not have permission to assign this role."},{status:403,headers:noStore})}const client=createServiceClient();const[{data:profile},{data:pending},authUsers]=await Promise.all([client.from("app_users").select("user_id,is_active").eq("email",email).maybeSingle(),client.from("user_invitations").select("id").eq("email",email).eq("status","pending").maybeSingle(),client.auth.admin.listUsers({page:1,perPage:1000})]);if(profile||authUsers.data.users.some(u=>u.email?.toLowerCase()===email))return NextResponse.json({error:profile&&!profile.is_active?"This account is disabled. Enable it instead of creating a duplicate.":"An account already exists for this email address."},{status:409,headers:noStore});if(pending)return NextResponse.json({error:"An invitation is already pending for this email address."},{status:409,headers:noStore});const{data:invitation,error:rowError}=await client.from("user_invitations").insert({email,assigned_role:role,invited_by:actor.userId}).select("id").single();if(rowError)return NextResponse.json({error:"Invitation could not be created."},{status:500,headers:noStore});const redirectTo=`${publicOrigin(request)}/auth/callback?next=/auth/setup-password`,{data:sent,error:sendError}=await client.auth.admin.inviteUserByEmail(email,{redirectTo,data:{kdi_invitation_id:invitation.id}});if(sendError||!sent.user){await client.from("user_invitations").update({status:"cancelled",cancelled_at:new Date().toISOString(),updated_at:new Date().toISOString()}).eq("id",invitation.id);await managementAudit(actor,"user_invited",{targetEmail:email,roleAfter:role,outcome:"failed",metadata:{reason:"email_provider_rejected"}});return NextResponse.json({error:"Invitation email could not be sent. Please try again later."},{status:502,headers:noStore})}const legacyRole=role==="admin"?"ADMIN":"STAFF";const now=new Date().toISOString(),{error:profileError}=await client.from("app_users").insert({user_id:sent.user.id,email,role:legacyRole,management_role:role,is_active:false,can_view_clinical:false,can_download:true,created_by:actor.userId,invited_by:actor.userId,invited_at:now});if(profileError){await managementAudit(actor,"user_invited",{targetUserId:sent.user.id,targetEmail:email,roleAfter:role,outcome:"failed",metadata:{reason:"profile_creation_failed"}});return NextResponse.json({error:"Invitation was sent but account setup could not be finalized. Contact support."},{status:500,headers:noStore})}await client.from("user_invitations").update({auth_user_id:sent.user.id,updated_at:now}).eq("id",invitation.id);await managementAudit(actor,role==="admin"?"admin_invited":"user_invited",{targetUserId:sent.user.id,targetEmail:email,roleAfter:role});return NextResponse.json({message:"Invitation sent successfully."},{status:201,headers:noStore})}
