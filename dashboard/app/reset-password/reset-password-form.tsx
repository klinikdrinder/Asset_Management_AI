"use client";import Link from"next/link";import{FormEvent,useState}from"react";import{createPasswordAuthClient}from"../lib/supabase/auth-client";export function ResetPasswordForm(){const[password,setPassword]=useState(""),[confirm,setConfirm]=useState(""),[error,setError]=useState(""),[done,setDone]=useState(false);async function submit(e:FormEvent){e.preventDefault();if(password.length<8){setError("Password must be at least 8 characters.");return}if(password!==confirm){setError("Passwords do not match.");return}const result=await createPasswordAuthClient(true).auth.updateUser({password});if(result.error){setError("The reset link is invalid or expired.");return}setDone(true)}return done?<div className="authSuccess">Password updated. <Link href="/login">Continue to Login</Link></div>:<form className="authForm" onSubmit={submit}><label>New Password<input type="password" autoComplete="new-password" minLength={8} required value={password} onChange={e=>setPassword(e.target.value)}/></label><label>Confirm Password<input type="password" autoComplete="new-password" minLength={8} required value={confirm} onChange={e=>setConfirm(e.target.value)}/></label>{error&&<div className="authError" role="alert">{error}</div>}<button className="authPrimary">Update Password</button></form>}
