"use client";export function LogoutButton(){async function logout(){await fetch("/api/admin-auth/logout",{method:"POST",credentials:"same-origin"}).catch(()=>undefined);window.location.replace("/login")}return <div className="accountActions"><a href="/account/change-password">Change Password</a><button className="signOutLink" type="button" onClick={logout}>Sign Out</button></div>}
