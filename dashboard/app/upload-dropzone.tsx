"use client";import{useState}from"react";export function UploadDropzone(){const[files,setFiles]=useState<File[]>([]),[done,setDone]=useState(false);return <section className="uploadPanel"><label className="uploadDrop"><input type="file" multiple accept="image/*,video/*,.pdf,.pptx" onChange={e=>{setFiles([...e.target.files??[]]);setDone(false)}}/><strong>Drop files here or choose files</strong><span>Images, videos and approved documents</span></label>{files.length>0&&<div className="uploadQueue">{files.map(f=><div key={`${f.name}-${f.size}`}><span>{f.name}</span><b>{Math.ceil(f.size/1024)} KB</b></div>)}<div className="uploadActions"><button onClick={()=>setDone(true)}>Run demo upload</button><button className="secondary" onClick={()=>{setFiles([]);setDone(false)}}>Cancel</button></div></div>}{done&&<div className="demoUploadResult">DEMO UPLOAD — NO PRODUCTION FILE WAS CREATED</div>}</section>}
