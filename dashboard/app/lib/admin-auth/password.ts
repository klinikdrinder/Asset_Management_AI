import"server-only";import bcrypt from"bcryptjs";import{PASSWORD_MIN_LENGTH}from"./constants";export function validatePassword(value:string){if(value.length<PASSWORD_MIN_LENGTH||value.length>256)throw new Error(`Password must be at least ${PASSWORD_MIN_LENGTH} characters.`)}export function hashPassword(value:string){validatePassword(value);return bcrypt.hash(value,12)}export function verifyPassword(value:string,hash:string){return bcrypt.compare(value,hash)}
