import{DevPreviewBanner}from"./dev-preview-banner";const links=[["Overview","/"],["Source Folders","/source-folders"],["Migration Results","/migration-results"],["Exceptions","/exceptions"],["Duplicates","/duplicates"],["System Status","/system-status"]];export function AppShell({active,children}:{active:string;children:React.ReactNode}){return <div className="shell"><DevPreviewBanner/><aside><div className="brand"><span className="brandMark">KDI</span><div>Central Media Library<small>Read-only operations</small></div></div><nav aria-label="Primary navigation">{links.map(([name,href])=><a key={href} href={href} className={active===href?"active":""}>{name}</a>)}</nav><div className="secureNote"><span>●</span> Secure preview view</div></aside><main>{children}</main></div>}export function Breadcrumbs({items}:{items:{label:string;href?:string}[]}){return <nav className="breadcrumbs" aria-label="Breadcrumb">{items.map((x,i)=><span key={`${x.label}-${i}`}>{i>0&&<b>/</b>}{x.href?<a href={x.href}>{x.label}</a>:<span aria-current="page">{x.label}</span>}</span>)}</nav>}export function Badge({children,tone="neutral"}:{children:React.ReactNode;tone?:string}){return <span className={`badge ${tone}`}>{children}</span>}export function PageError({message}:{message:string}){return <div className="state"><span className="stateIcon">!</span><h2>{message}</h2><p>No internal diagnostics or credentials have been displayed.</p></div>}
