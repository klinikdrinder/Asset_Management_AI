import Link from"next/link";import{PasswordLoginForm}from"./password-login-form";export const dynamic="force-dynamic";export default async function LoginPage({searchParams}:{searchParams:Promise<{next?:string}>}){const{next}=await searchParams;return <main className="authPage"><section className="authCard"><Link className="authBrand" href="/"><span>KDI</span><div>Central Media<small>Library</small></div></Link><div className="authKicker">SECURE SIGN IN</div><h1>Welcome back</h1><p>Sign in with your approved KDI email account.</p><PasswordLoginForm next={next}/><small className="authNotice">Access is limited to approved Staff and Administrator accounts.</small></section></main>}
