import assert from"node:assert/strict";import test from"node:test";import{interpretQuery}from"../db/query-interpreter";test("semantic metamorphic equivalents",async()=>{const a=await interpretQuery("implanting grafts"),b=await interpretQuery("implantation of grafts");assert.deepEqual(a.semantic.positive_concepts.map(x=>x.canonical_code),b.semantic.positive_concepts.map(x=>x.canonical_code))});
