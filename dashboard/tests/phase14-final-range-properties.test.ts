import assert from"node:assert/strict";import test from"node:test";import{interpretQuery}from"../db/query-interpreter";test("typed range families",async()=>{for(const q of["between 25 and 35 years old","between 2000 and 3000 grafts","between 20 and 40 seconds"]){const p=await interpretQuery(q);assert.equal(p.validation.status,"VALID");assert.equal(p.result_request.requested_count,null)}});
