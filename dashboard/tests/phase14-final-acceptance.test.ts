import assert from"node:assert/strict";import{readFileSync}from"node:fs";import path from"node:path";import test from"node:test";import{validateExpectation}from"../lib/search/query-parser-benchmark-evaluator";import{validateGold}from"../lib/search/query-parser-gold-validator";test("final benchmark is frozen and independently gold-valid",()=>{const b=JSON.parse(readFileSync(path.resolve(process.cwd(),"..","config","semantic-search","benchmarks","kdi_query_parser_benchmark_final_v3.json"),"utf8"));assert.equal(b.benchmark_version,"kdi_query_parser_benchmark_final_v3");assert.equal(b.frozen,true);assert.equal(b.cases.length,240);assert.deepEqual(b.split_counts,{DEV:160,VALIDATION:40,BLIND:40});for(const c of b.cases){assert.deepEqual(validateExpectation(c.expected),[]);assert.equal(validateGold(c.query,c.expected).valid,true,c.case_id)}});
