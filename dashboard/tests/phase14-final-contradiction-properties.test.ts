import assert from"node:assert/strict";import test from"node:test";import{interpretQuery}from"../db/query-interpreter";test("canonical contradiction families",async()=>{for(const q of["show 4 files then show 9 videos","only images but videos","PNG videos","no injection but injection must be visible"]){assert.ok((await interpretQuery(q)).parser_metadata.conflicts.length)}});
