import test from"node:test";import assert from"node:assert/strict";import{readFileSync}from"node:fs";import{oracle,type Facts}from"../scripts/phase18-protected-oracle";const base:Facts={authenticated:true,valid_user:true,user_active:true,user_clinical:false,user_download:false,asset_available:true,source_available:true,source_missing:false,source_trashed:false,auth_row:true,internal:"ALLOWED",clinical:false,requires_clinical:false,sensitivity:"GENERAL",download_allowed:false};
test("oracle is independent and conservative",()=>{assert.deepEqual(oracle(base),{discover:true,view_metadata:true,preview:true,download:false});for(const x of[{authenticated:false},{user_active:false},{asset_available:false},{source_available:false},{auth_row:false},{internal:"UNKNOWN"as const},{clinical:null}])assert.equal(oracle({...base,...x}).discover,false)});test("oracle source does not import or invoke production authorization",()=>{const s=readFileSync("scripts/phase18-protected-oracle.ts","utf8");for(const p of["can_user_view_asset_for","can_user_download_asset_for","candidate-authorizer","media-service"])assert.doesNotMatch(s,new RegExp(p))});
