import assert from"node:assert/strict";import test from"node:test";import{interpretQuery}from"../db/query-interpreter";test("nominalized and passive actions",async()=>{for(const q of["implantation of grafts","grafts being implanted","drawing of hairline","scalp being cleansed"]){assert.ok((await interpretQuery(q)).semantic.positive_concepts.some(x=>x.concept_type==="ACTION"),q)}});
