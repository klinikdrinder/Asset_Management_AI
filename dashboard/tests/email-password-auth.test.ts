import test from"node:test";import assert from"node:assert/strict";import{readFileSync,existsSync}from"node:fs";const read=(p:string)=>readFileSync(p,"utf8");
test("custom administrator login and recovery pages are visible without Google OAuth",()=>{for(const path of["app/login/page.tsx","app/login/password-login-form.tsx","app/forgot-password/page.tsx","app/forgot-password/forgot-password-form.tsx","app/account/change-password/page.tsx"])assert.equal(existsSync(path),true,path);const login=read("app/login/page.tsx")+read("app/login/password-login-form.tsx");for(const value of[/Email/,/Password/,/Remember Me/,/Login/,/Forgot password/,/\/api\/admin-auth\/login/])assert.match(login,value);assert.doesNotMatch(login,/GoogleLoginButton|Continue with Google/)});
test("password recovery change and sign out use custom server-only routes",()=>{const forgot=read("app/forgot-password/forgot-password-form.tsx"),change=read("app/account/change-password/change-password-form.tsx"),logout=read("app/logout-button.tsx"),shell=read("app/application-shell.tsx");assert.match(forgot,/Recovery Key/);assert.match(forgot,/\/api\/admin-auth\/recover/);assert.match(change,/\/api\/admin-auth\/change-password/);assert.match(logout,/\/api\/admin-auth\/logout/);assert.match(logout,/Sign Out/);assert.match(shell,/user\?\.email/)});
test("temporary bypass remains explicit and disabled configuration is documented",()=>{const config=read("app/lib/local-auth-bypass-config.ts"),example=read(".env.example"),proxy=read("proxy.ts");assert.match(config,/LOCAL_AUTH_BYPASS === "true"/);assert.match(example,/LOCAL_AUTH_BYPASS=false/);assert.match(proxy,/isLoopbackHost/);assert.match(proxy,/updateSession\(request\)/)});
