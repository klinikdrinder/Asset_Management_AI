import assert from"node:assert/strict";import test from"node:test";import{interpretQuery}from"../db/query-interpreter";import{classifyRequirements}from"../db/requirement-classifier";test("parser conflicts survive",async()=>{for(const q of["show 4 files then show 9 videos","only images but videos","PNG videos","no injection but injection must be visible"]){const p=await interpretQuery(q),r=classifyRequirements(p);assert.equal(r.conflicts.length,p.parser_metadata.conflicts.length);assert.ok(r.conflicts.length)}});
