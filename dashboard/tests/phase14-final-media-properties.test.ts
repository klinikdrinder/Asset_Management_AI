import assert from"node:assert/strict";import test from"node:test";import{interpretQuery}from"../db/query-interpreter";test("media and extension families",async()=>{assert.equal((await interpretQuery("generic material")).media.media_type,"ANY");assert.equal((await interpretQuery("PNG files")).media.media_type,"IMAGE");assert.equal((await interpretQuery("MOV assets")).media.media_type,"VIDEO")});
